import ReactDOM from 'react-dom/client';
import './index.css';         // ✅ volvemos al pipeline normal
import App from './App';

ReactDOM.createRoot(document.getElementById('root')!).render(<App />);
